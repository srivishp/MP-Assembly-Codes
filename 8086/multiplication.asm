ASSUME CS:CODE, DS:DATA

DATA SEGMENT
	ORG 3000H
	NUM1 DW 33FAH
	NUM2 DW 631FH
	RES DW 2 DUP(0)
DATA ENDS

CODE SEGMENT
START:	MOV AX, DATA
	MOV DS, AX
	MOV AX, NUM1
	MOV BX, NUM2
	IMUL BX
	MOV RES, AX
	MOV RES+2, DX
	MOV AH, 4CH
	INT 21H
CODE ENDS
END START
